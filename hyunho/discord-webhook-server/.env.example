# Discord Webhook 환경변수 설정
# Google Cloud Functions에서는 Console에서 환경변수 설정

# 웹훅 URL 패턴: WEBHOOK_채널명=Discord_웹훅_전체_URL
WEBHOOK_GENERAL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN
WEBHOOK_ALERTS=https://discord.com/api/webhooks/ALERT_WEBHOOK_ID/ALERT_WEBHOOK_TOKEN  
WEBHOOK_LOGS=https://discord.com/api/webhooks/LOG_WEBHOOK_ID/LOG_WEBHOOK_TOKEN

# Google Cloud 배포 시 설정 방법:
# gcloud functions deploy discord-webhook-server \
#   --set-env-vars WEBHOOK_GENERAL="실제_웹훅_URL" \
#   --set-env-vars WEBHOOK_ALERTS="실제_웹훅_URL"

# 사용법:
# POST /api/webhook/general    (환경변수 이름 사용)
# POST /api/webhook/alerts     (환경변수 이름 사용)
# POST /api/webhook/webhook_id/token  (직접 ID/토큰 사용)